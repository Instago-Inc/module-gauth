## v1.0.3 - Docs clarity and dependency refresh
Clarifies how `gauth` caches tokens and what configuration flows mean while aligning helper dependencies with the current `http` and `json` releases. Workflow authors immediately see the refreshed docs and dependency defaults.

### Changed
- Reworded README and documentation sections so token caching, configuration, and scope fallbacks are clearer to gauth users.
- Updated the helper imports to `http@latest` and `json@latest` so the default dependencies stay current with the runtime ecosystem.

## v1.0.2 - Clearer gauth docs
Clarifies `gauth` messaging across README and docs so workflow authors quickly understand how token caching and configuration behave.

### Changed
- Reworded README and documentation sections to highlight consistent behavior, token reuse, and configuration clarity.

## v1.0.1 - Maintenance release metadata
Maintenance release for gauth with release metadata updates and versioning keywords.

### Changed
- Version bump and changelog entry for release consistency.
